services:
  ollama:
    command: 'ollama run mistral'
    container_name: 'ollama'
    image: 'ollama/ollama:latest'
    ports:
      - '11434:11434'
    volumes:
      - '.:/code'
      - './ollama:/root/.ollama'
